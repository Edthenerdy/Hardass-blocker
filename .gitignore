# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Local Claude Code project state
.claude/

# Build artifacts
*.zip
dist/

# Server runtime state (seeded on first run)
server/data.json

# Extension signing key (never commit private keys)
.keys/

# Environment / secrets (Stripe keys etc.) — never commit
.env
.env.local
