{
  "name": "hardass-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling for Hardass Blocker — icon generation and the Chrome↔backend integration test",
  "scripts": {
    "test:chrome": "node chrome-integration-test.js",
    "icons": "node gen-icons.js"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  },
  "engines": {
    "node": ">=20"
  }
}
